#include "main.h"

/**
 * _abs - checks if the number is absolute or not.
 *
 * @n: is an integer argument
 *
 * Return: Always 0.
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
